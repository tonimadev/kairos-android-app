package digital.tonima.core.prefs

/**
 * Shared preference names and keys used across app, core and wear modules.
 */
object PrefsConstants {
    const val ALARM_PREFS = "AlarmPrefs"
    const val KEY_GLOBAL_ALARMS_ENABLED = "global_alarms_enabled"
    const val KEY_DISABLED_EVENT_IDS = "disabled_event_ids"
    const val KEY_ALARM_OFFSET_MINUTES = "alarm_offset_minutes"
}
